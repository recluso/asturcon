---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

{date && (
	<time datetime={date.toISOString()}>
		{
			date.toLocaleDateString('en-gb', {
				year: 'numeric',
				month: 'long',
				day: 'numeric',
			})
		}
	</time>
)}